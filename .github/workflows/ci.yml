name: CI

# The gate. It runs the same command a maintainer runs before a commit: typecheck,
# unit tests, build, drift check against the committed dist, and the browser suite.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    name: gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install poppler-utils, which the print tests read the PDF with
        run: sudo apt-get update && sudo apt-get install --yes --no-install-recommends poppler-utils

      - name: Install the Chromium build Playwright pins
        run: npx playwright install --with-deps chromium

      - name: Run the gate
        run: npm test

      - name: Upload browser artefacts on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-artefacts
          path: test/.artefacts/
          retention-days: 7
          if-no-files-found: ignore
